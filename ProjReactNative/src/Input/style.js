import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    input: {
        backgroundColor: '#fff',
        borderWidth: 0.5,
        borderColor: '#d6d7da',
        padding: 15,
        marginVertical: 10
    }
});

export default styles;